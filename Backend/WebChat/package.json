{
  "name": "bed_ca1_2023_s2_zy",
  "version": "1.0.0",
  "description": "```\r CREATE DATABASE `SP_Tasks`;\r USE `SP_Tasks`;\r CREATE TABLE `User` (\r  `user_id` INT PRIMARY KEY AUTO_INCREMENT,\r  `username` TEXT,\r  `email` TEXT\r );\r CREATE TABLE `Task` (\r  `task_id` INT PRIMARY KEY AUTO_INCREMENT,\r  `title` TEXT,\r  `description` TEXT,\r  `points` INT\r );\r CREATE TABLE `TaskProgress` (\r  `progress_id` INT PRIMARY KEY AUTO_INCREMENT,\r  `user_id` INT NOT NULL,\r  `task_id` INT NOT NULL,\r  `completion_date` TIMESTAMP,\r  `notes` TEXT\r );",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "mysql": "^2.18.1",
    "nodemon": "^3.0.2",
    "redis": "^4.6.12"
  }
}
